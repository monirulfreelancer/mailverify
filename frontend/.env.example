# Base URL of the mailverify backend API.
# All requests are sent to ${VITE_API_URL}/api/v1/...
# Vite inlines VITE_* vars at BUILD time, so set this before `npm run build`
# (and before `npm run dev` for local development).
VITE_API_URL=http://localhost:3000
